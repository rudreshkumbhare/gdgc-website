# URL of the deployed backend API.
# Leave unset for local development (defaults to http://localhost:5001).
# In production (Vercel/Netlify/etc.), set this to your deployed backend's URL:
# VITE_API_URL=https://your-backend.onrender.com
